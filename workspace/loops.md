# Active Loops

> If the container restarts, CronList will return empty. Recreate any rows below using `/loop [interval] <prompt>`.
> Update this file whenever a loop is created or deleted.

| ID | Interval | Prompt | Created |
|----|----------|--------|---------|
