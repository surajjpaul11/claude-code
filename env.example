# Anthropic API Key (optional — you can use /login inside the container instead)
# Get one at: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-your_key_here

# GitHub Personal Access Token (required for GitHub MCP + git push/pull)
# Create one at: https://github.com/settings/tokens
# Needed scopes: repo, read:org, read:user
GITHUB_TOKEN=ghp_your_token_here

# GitHub Classic PAT (required for repo creation via create_new_project.sh)
# Create one at: https://github.com/settings/tokens (classic)
# Needed scopes: repo
GH_PAT=ghp_your_classic_pat_here

# Git identity for commits
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com
