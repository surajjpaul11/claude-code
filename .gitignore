# Environment / secrets — never commit these
.env

# macOS
.DS_Store

# Editor
.vscode/
*.swp
*.swo

# Node
node_modules/

# Cloned projects in workspace (each has its own git repo)
workspace/*/
!workspace/repo_clone_instructions.md
